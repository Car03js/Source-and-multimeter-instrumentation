# About this project.

This is a school project wher it simulates a IC test plan line, where the idea is that multiple PCB's are tested to ensure they follow the standard specification values found in datasheets of the circuit. So it must have set ranges found from a database developed in SQL Server.
